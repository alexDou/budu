export enum NotificationType {
    invite = 'invite',
    rating = 'rating',
    review = 'review',
    chat = 'chat',
}

export enum NotificationStatus {
    seen = 'seen',
    pending = 'pending',
}